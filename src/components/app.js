import React from 'react';
import { Component } from 'react';

export default class App extends Component {
  render () {
    return (
      <div>
        React-Boilerplate with redux, react-router, and proxy
      </div>
    );
  }
};